# 3. Пользовательские истории (US)

В этом разделе описаны ключевые пользовательские сценарии (US1–US7), согласованные
с системой состояний FSM и справочниками кнопок/текстов.

Во всех местах, где упоминаются `<label ...>` и `<message ...>`, подразумевается
ссылка на:
- `app/locales/ru/buttons.py`
- `app/locales/ru/texts.py`


---

## US1. Анализ блюда (фото + текстовые уточнения)

### 1.1. Вход в сценарий

#### Вариант 1: через главную страницу

1. Пользователь находится в состоянии `STATE_STANDARD`.
2. Пользователь нажимает кнопку `<label analyze_food>` — «📸 Анализировать еду».
3. Бот отправляет сообщение `<message send_photo_for_analysis>`:

   > 📸 Отправьте фото еды для анализа

4. Состояние FSM: `STATE_PHOTO_COMMENT` (режим ввода комментариев к фото).

#### Вариант 2: прямое отправление фото

1. Пользователь находится в `STATE_STANDARD`.
2. Пользователь отправляет **фото** в чат.
3. Бот:
   - Проверяет тип сообщения:
     - Если это **photo** → принимает для анализа
     - Если это **document** → отвечает:

       `<message document_received>` (нужно добавить в справочник):

       > 📄 Я получил файл, но для анализа нужна фотография.  
       > Отправьте фото через кнопку «Фото» в Telegram.

     - Если это не фото (текст, аудио, стикер) → отвечает подсказкой:

       `<message photo_first_then_text>`:

       > 📸 Сначала отправьте фото, затем можете уточнить текстом

4. При успешном получении фото:
   - Бот сохраняет ссылку/файл_id фото в контексте сессии
   - Выводит меню анализа:
     - `<label nutrition>` — «Калорийность»
     - `<label recipe>` — «Рецепт»
     - `<label new_photo>` — «Новое фото»
     - `<label back>` — «Главная»
   - Переводит пользователя в `STATE_PHOTO_COMMENT`.


### 1.2. Работа в сессии анализа (`STATE_PHOTO_COMMENT`)

#### Основная логика

- Сессия анализа привязана к:
  - user_id
  - последнему фото
  - списку комментариев (текстовых сообщений)
- Пользователь может писать текстовые сообщения подряд, уточняя:
  - состав блюд
  - вес, сахар, масло, гарниры и т.п.
- Каждое текстовое сообщение:
  - добавляется в список «комментариев к фото»
  - увеличивает счетчик сообщений в рамках текущего фото

#### Лимиты и защиты

- **Лимит сообщений к одному фото:** максимум **10** текстовых сообщений.
  - При достижении лимита бот отправляет:

    `<message max_messages_for_photo>`:

    > ⚠️ Вы достигли максимума сообщений (10) для этого фото.  
    > Автоматически запускаю анализ.

  - После этого бот **сам вызывает** анализ (как будто нажата кнопка `<label nutrition>` или `<label recipe>`, в зависимости от последнего выбора/режима по умолчанию).
- **Тайм-аут сессии:** 60 минут неактивности.
  - Если от пользователя нет ни одного сообщения/кнопки 60 минут:
    - Бот очищает контекст сессии (фото, комментарии, счетчики)
    - Переводит пользователя в `STATE_STANDARD`
    - Отправляет `<message session_expired>`:

      > ⏰ Сессия истекла (более 1 часа неактивности). Начните заново.
- **Фильтрация “мусорных” сообщений:**
  - Если текст **явно не про еду** (мат, повторяющиеся бессмысленные последовательности, спам-символы и т.п.), бот:
    - Не использует текст в промпте GPT
    - Считает это попыткой «уточнения» (для защиты от злоупотреблений)
    - Отвечает:

      `<message off_topic_warning>` (нужно добавить):

      > Я могу помочь только с анализом блюд на фото.  
      > Пожалуйста, задайте вопросы о питательной ценности или составе еды.

- **Антифлуд по фото:**
  - Если пользователь 3 раза подряд:
    - отправляет фото
    - и к каждому фото — длинную последовательность мусорных сообщений
  - можно добавить мягкий бан:
    - перевести пользователя в режим только чтения (необязательная реализация в первой версии, можно оформить в `abuse_protection.md`).


### 1.3. Нажатие «📊 Калорийность»

1. Пользователь нажимает `<label nutrition>` — «Калорийность».
2. Бот проверяет:
   - Есть ли активное фото в текущей сессии
   - Не превышен ли лимит по дневным анализам:
     - Бесплатный: 5 фото/день
     - Премиум: 15 фото/день
3. Если лимит превышен:
   - Бот отправляет `<message daily_limit_exceeded>` или `<message limit_exceeded_detailed>`.
   - Показывает под сообщением кнопку `<label buy_premium>` где уместно.
   - Состояние: `STATE_STANDARD`.
4. Если лимит не превышен:
   - Бот отправляет `<message analyzing_image>`:

     > 🔍 Анализирую изображение...

   - Формирует системный промпт `SYSTEM_PROMPT_NUTRITION` (см. `app/prompts/food_analysis.py`)
   - Передает в GPT:
     - Фото
     - Все собранные текстовые комментарии (отфильтрованные)
   - Получает от GPT ответ с:
     - суммарной калорийностью и БЖУ
     - разбивкой по блюдам и напиткам
     - возможным уточняющим вопросом (с эмодзи ⁉️)

5. Бот отправляет ответ в чат как есть (форматируется GPT, см. промпт).
6. Если у пользователя тариф Премиум:
   - под сообщением появляется кнопка `<label ate_this>` — «Я это съел».
7. FSM остаётся в `STATE_PHOTO_COMMENT`, пока:
   - пользователь отправляет уточняющие сообщения
   - или нажимает `<label new_photo>` / `<label back>`


### 1.4. Нажатие «👨‍🍳 Рецепт»

1. Пользователь нажимает `<label recipe>` — «Рецепт».
2. Логика аналогична пункту 1.3, но:
   - Используется системный промпт `SYSTEM_PROMPT_RECIPE`.
   - Результат содержит:
     - список блюд
     - ингредиенты
     - способ приготовления
     - время и порции

3. Формат ответа (генерирует GPT, см. `SYSTEM_PROMPT_RECIPE`):

   ```text
   👨‍🍳 Рецепт: Название блюда

   🛒 Ингредиенты:
   - Ингредиент 1: количество
   - Ингредиент 2: количество

   📝 Приготовление:
   1. Шаг 1
   2. Шаг 2

   ⏱️ Время: 30 минут
   🍽️ Порций: 2
   ```

4. Пользователь может отправлять уточнения по рецепту в рамках той же сессии (до лимита уточнений).

### 1.5. Уточнения после анализа (повторные запросы к GPT)

- После первого анализа (калорийность или рецепт) пользователь может отправить текстовое сообщение:
  - уточнить состав блюда
  - запросить замену ингредиентов
  - уточнить соус, гарнир и т.п.
- Каждое такое сообщение:
  - считается «уточнением» и уменьшает лимит:
    - Бесплатный: 2 уточнения
    - Премиум: 5 уточнений
- После каждого уточнения бот показывает:

  `<message refinement_counter>` (добавить):

  > Вы использовали {used}/{limit} уточнений.

- Если GPT определяет, что сообщение **не про еду**:
  - бот отвечает `<message off_topic_warning>`
  - попытка уточнения **всё равно списывается** (чтобы защититься от злоупотреблений)
- После исчерпания лимита уточнений:
  - бот отправляет `<message refinement_limit_reached>` (добавить):

    > Лимит уточнений исчерпан. Отправьте новое фото для анализа.

  - дальнейшие сообщения в рамках `STATE_PHOTO_COMMENT` не вызывают GPT, пока не придет новое фото.


### 1.6. Кнопки «Новое фото» и «Главная»

- `<label new_photo>` — «Новое фото»:
  - Очищает контекст текущей сессии (фото, комментарии, счетчики).
  - Остаётся состояние `STATE_PHOTO_COMMENT`.
  - Бот отправляет `<message send_photo_for_analysis>`.

- `<label back>` — «Главная»:
  - Очищает контекст текущей сессии анализа.
  - Переводит пользователя в `STATE_STANDARD`.
  - Показывает главную страницу (кнопки: анализ, помощь, профиль, отчеты, купить премиум).


---

## US2. Просмотр помощи

### 2.1. Вход в сценарий

1. Пользователь в любом состоянии **кроме** узкоспециализированного ввода (например, `STATE_PROMO` / `STATE_CALORIES_PLAN`) нажимает кнопку `<label help>` — «Помощь».
2. Бот отправляет `<message help_text>`:

   ```text
   👋 Привет! Это Food Lens — бот для анализа еды по фото.

   📸 Вы можете:
   - Получить калорийность блюда
   - Узнать рецепт
   - Добавлять текстовые уточнения к фото

   💎 Бесплатный тариф:
   - 5 фото в день
   - 2 попытки уточнений
   - Анализ калорийности и рецепта
   - Реклама

   💎 Платный тариф:
   - 15 фото в день
   - 5 попыток уточнений
   - Анализ калорийности и рецепта
   - Премиум отчеты за день, неделю, месяц
   - Отсутствие рекламы

   ⚠️ Важно:
   - Информация приблизительная, бот не является медицинским экспертом
   - История сообщений и фото не сохраняется
   - Нарушение лимитов или попытки обхода ограничений могут привести к блокировке функций

   Используйте Food Lens для удобного контроля питания.
   Мы не несём ответственности за последствия использования информации.
   ```

   (Текст хранится в `RussianTexts.texts['help_text']` с аккуратными переносами строк.)

3. FSM-состояние:
   - Если до этого было `STATE_STANDARD` → остаётся `STATE_STANDARD`
   - Если было `STATE_PHOTO_COMMENT` → остаётся `STATE_PHOTO_COMMENT` (помощь — вспомогательный текст)
   - Если пользователь был в критическом режиме ввода (промокод, план калорий), **рекомендуется**:
     - Либо игнорировать кнопку помощь
     - Либо показать краткую help-подсказку, не сбрасывая режим

### 2.2. Выход из сценария

- Любое дальнейшее действие пользователя:
  - нажатие любой кнопки (кроме help)
  - отправка фото
- → приводит к соответствующей логике других US (анализ, профиль, отчеты и т.п.).


---

## US3. Просмотр профиля

### 3.1. Вход в сценарий

1. Пользователь нажимает `<label profile>` — «Профиль».
2. FSM-состояние: `STATE_STANDARD` (профиль — «просмотр», не спец. режим ввода).
3. Бот формирует данные профиля:
   - тип подписки (бесплатный / премиум)
   - дата окончания премиума (если активен)
   - использованные запросы за сегодня
   - общий лимит на день
   - при наличии — план по калориям
4. Бот отправляет комбинацию сообщений на основе текстов:

   - `<message profile_subscription_premium>` или `<message profile_subscription_free>`
   - `<message profile_premium_until>` — если премиум
   - `<message profile_used_today>`
   - `<message profile_remaining>`
   - для бесплатных пользователей можно добавить:

     `<message profile_premium_features>` (добавить):

     > ✅ Купите премиум подписку и получите:  
     > • 15 анализов в день  
     > • Отчеты за день, неделю, месяц  
     > • Отсутствие рекламы

5. Бот отображает кнопки:
   - `<label analyze_food>`
   - `<label help>`
   - `<label reports>`
   - `<label calorie_plan>`
   - `<label buy_premium>` (если тариф бесплатный)


### 3.2. Выход из сценария

- Нажатие любой кнопки:
  - `<label analyze_food>` → US1
  - `<label reports>` → US6
  - `<label calorie_plan>` → US5
  - `<label buy_premium>` → US4
  - `<label help>` → US2
- Отправка фото → US1 (анализ блюда).


---

## US4. Оплата подписки и промокоды

### 4.1. Экран оплаты

1. Пользователь нажимает `<label buy_premium>` — «Купить премиум»:
   - на главной
   - в профиле
   - под сообщением о лимитах
2. Бот отображает кнопки:
   - `<label buy_month_confirm>` — «Купить премиум на месяц»
   - `<label buy_week_confirm>` — «Купить премиум на неделю`
   - `<label enter_promo>` — «Ввести промокод»
   - `<label back>` — «Главная»
3. Бот отправляет `<message premium_info>` / отдельный текст:

   > Вы можете оплатить подписку звездами Telegram или ввести промокод.  
   > Премиум подписка включает:  
   > • 15 анализов в день  
   > • Отсутствие рекламы  
   > • Отчеты за день, неделю, месяц

FSM при этом остаётся в `STATE_STANDARD`.


### 4.2. Оплата через Telegram Stars

1. Пользователь нажимает:
   - `<label buy_month_confirm>` или
   - `<label buy_week_confirm>`.
2. Бот открывает окно оплаты через Telegram Stars.
3. После успешной оплаты:
   - Система:
     - активирует премиум-режим
     - записывает дату окончания подписки (неделя/месяц)
     - сбрасывает текущие лимиты запросов
   - Бот отправляет `<message promo_success>` или отдельный текст:

     > Поздравляем! Вы приобрели премиум подписку на {период}.  
     > Она завершится {дата}.  
     > Теперь доступны функции:  
     > • 15 анализов в день  
     > • Отчеты за день, неделю, месяц  
     > • Отсутствие рекламы

   - После этого бот открывает профиль (US3).
4. Если пользователь закрыл окно оплаты без оплаты:
   - остаётся в `STATE_STANDARD`
   - можно вернуть на главную по `<label back>`.

### 4.3. Ввод промокода (`STATE_PROMO`)

1. Пользователь нажимает `<label enter_promo>` — «Ввести промокод».
2. Бот:
   - Переводит пользователя в `STATE_PROMO`.
   - Отправляет `<message promo_input_prompt>` (добавить):

     > Отправьте промокод для активации премиум подписки.

3. В `STATE_PROMO` **любое текстовое сообщение**:
   - интерпретируется как кандидат промокода
   - проверяется:
     - существует ли промокод
     - не истек ли срок действия
     - не был ли он использован ранее (этим или другим пользователем)
     - не находится ли пользователь в бане по вводу промокодов

4. Если промокод **валиден**:
   - Система:
     - активирует премиум режим
     - выставляет дату окончания по правилам промокода (неделя/месяц и т.п.)
     - сбрасывает лимиты
   - Бот отправляет `<message promo_success>`:

     > 🎉 Промокод успешно активирован!  
     > У вас включен премиум до {until}.

   - FSM: возвращаем в `STATE_STANDARD`
   - Можно автоматически открыть профиль (US3).

5. Если промокод **невалиден / уже использован**:
   - Бот отправляет `<message promo_invalid>`:

     > ❌ Неверный или уже использованный промокод. Введите другой.

   - Увеличивает счетчик неудачных попыток:
     - 10 неверных попыток подряд → бан на 30 минут
     - ещё 10 после окончания бана → бан на сутки
     - ещё 10 → бан на неделю
   - При наложении бана бот отправляет `<message promo_banned_until>` (добавить):

     > Промокод был введен неверно 10 раз.  
     > Попробуйте снова после {datetime}.

6. Выход из `STATE_PROMO`:
   - Ввод валидного промокода
   - Нажатие любой другой кнопки (`<label back>`, `<label analyze_food>`, и т.п.) — перевод в соответствующий сценарий + `STATE_STANDARD`.


---

## US5. Редактирование плана по калориям (`STATE_CALORIES_PLAN`)

### 5.1. Вход в сценарий

1. Пользователь нажимает:
   - на главной: `<label set_calorie_goal>` (если выведена), или
   - в профиле: `<label calorie_plan>` — «План калорий».
2. Бот переводит пользователя в `STATE_CALORIES_PLAN`.
3. Бот отправляет `<message calories_plan_prompt>` (добавить):

   > Введите ваш дневной план по калориям.  
   > Отправьте одно число (Ккал) от 0 до 10000.

### 5.2. Обработка ввода

1. В `STATE_CALORIES_PLAN` любое текстовое сообщение:
   - проверяется на соответствие **целому числу 0–10000**.
2. Если введено:
   - **не число** или вне диапазона:
     - Бот отправляет `<message calories_plan_invalid>`:

       > ❌ Ввести калории нужно числом от 0 до 10000.

     - Остаётся в `STATE_CALORIES_PLAN`.
   - **0**:
     - Бот сбрасывает план (не учитывать при формировании отчетов).
     - Отправляет `<message calories_plan_reset>`:

       > План по калориям сброшен.  
       > В отчетах план учитываться не будет.

     - Переводит пользователя в `STATE_STANDARD` и открывает профиль (по желанию).
   - Значение **1–10000**:
     - Бот сохраняет план в БД.
     - Отправляет `<message calories_plan_saved>`:

       > 🎯 План по калориям {value} Ккал сохранен.

     - Переводит пользователя в `STATE_STANDARD` и открывает профиль.


### 5.3. Выход из сценария

- Пользователь может прервать ввод:
  - нажав любую кнопку меню (например, `<label back>` — «Главная»)
- В этом случае:
  - FSM: `STATE_STANDARD`
  - Изменения плана не происходят (если не было корректного ввода).


---

## US6. Просмотр отчетов

### 6.1. Общий сценарий

1. Пользователь нажимает `<label reports>` — «Отчеты».
2. Бот:
   - проверяет, есть ли у пользователя премиум-подписка:
     - если **нет**:
       - может показать промо-текст:

         `<message premium_reports_info>` (добавить):

         > Отчеты доступны только в премиум подписке.  
         > Купите премиум, чтобы видеть статистику за день, неделю и месяц.

       - и вывести кнопку `<label buy_premium>`
       - FSM: `STATE_STANDARD`
     - если **да** (премиум):
       - показывает кнопки:
         - `<label report_day>` — «Отчет за день»
         - `<label report_week>` — «Отчет за неделю»
         - `<label report_month>` — «Отчет за месяц»
         - `<label back>` — «Главная»
       - FSM: `STATE_STANDARD` (отчеты — чистый просмотр).

3. Отчеты строятся по блюдам, по которым пользователь нажал `<label ate_this>`.


### 6.2. Отчет за день

1. Пользователь нажимает `<label report_day>`.
2. Бот:
   - Собирает из БД блюда за текущие сутки (00:00–23:59) с флагом «съедено».
   - Считает:
     - суммарные калории
     - суммарные БЖУ
     - сравнивает с планом (если есть).
3. Формат сообщения (пример, может быть в `<message report_day_template>`):

   ```text
   Пн, 10 марта 2025

   🔥 1800 Ккал из 2000 Ккал

   🔼 Перебор: 200 Ккал
   # или
   🔽 Недобор: 200 Ккал

   🍗 Б: 120г  🥑 Ж: 60г  🍚 У: 190г

   Омлет с сыром: 250г 🔥 400 Ккал
   Салат Цезарь: 200г 🔥 350 Ккал
   Борщ: 300г 🔥 300 Ккал
   ...
   ```

4. Если план по калориям **не задан**:
   - Бот не показывает блоки «перебор/недобор».

5. Выход:
   - `<label back>` → главная
   - Повторный вызов отчета или выбор другой кнопки → соответствующий сценарий.


### 6.3. Отчет за неделю

1. Пользователь нажимает `<label report_week>`.
2. Бот:
   - Определяет текущую неделю (с понедельника по текущий день).
   - По каждому дню:
     - считает фактические калории
     - сравнивает с планом (если есть)
3. Формат (пример):

   ```text
   Отчет за неделю: 3–9 марта 2025

   🔼 Перебор по калориям: 3 дня
   🔽 Недобор по калориям: 2 дня

   Пн, 3 марта — 🔥 2100 / 2000 Ккал 🔼 Перебор 100 Ккал
   Вт, 4 марта — 🔥 1800 / 2000 Ккал 🔽 Недобор 200 Ккал
   Ср, 5 марта — 🔥 2000 / 2000 Ккал ✅ В норме
   ...
   ```

4. По желанию можно не расписывать блюда по каждому дню (для простоты первой версии).

### 6.4. Отчет за месяц

1. Пользователь нажимает `<label report_month>`.
2. Бот:
   - Берет период с 1-го числа текущего месяца по текущий день.
   - Если начало месяца позже, чем дата первого зафиксированного приема пищи, можно:
     - стартовать с первой даты в базе.
3. Формат (пример):

   ```text
   Отчет за месяц: март 2025

   🔼 Перебор по калориям: 10 дней
   🔽 Недобор по калориям: 12 дней

   Средний дневной калораж: 1850 Ккал при плане 2000 Ккал
   ```

4. Выход: `<label back>` → главная.


---

## US7. Админка приложения

### 7.1. Вход в админку (`STATE_ADMIN`)

1. Администратор отправляет команду `/superadmin`.
2. Бот:
   - Проверяет user_id по списку админов из `.env` или конфига.
   - Если user_id **не в списке**:
     - Отправляет `<message admin_access_denied>`:

       > ⛔️ Команда доступна только администраторам.

     - Остается в `STATE_STANDARD`.
   - Если user_id **в списке**:
     - Переводит в `STATE_ADMIN`.
     - Отправляет текст со сводной статистикой (можно оформить в отдельный `<message admin_dashboard>`):
       - За месяц новых уникальных пользователей: N
       - Оплатили премиум: N
       - Отправлено фото: N
       - Пользователей заходивших более 1 раза: N
       - Пользователей заходивших более 1 раза за 5 дней: N
       - Токенов потрачено: ...
       - Рекламы показано: ...
       - Потрачено в GPT API: ...
       - Маржа за неделю: ...
     - Показывает кнопки:
       - `<label admin_statistics>`
       - `<label admin_manage_limits>`
       - `<label admin_exit>`


### 7.2. Управление лимитами (`STATE_LIMIT_RESET`, часть `STATE_ADMIN`)

1. Админ нажимает `<label admin_manage_limits>`.
2. Бот показывает кнопки:
   - `<label admin_sub_toggle_premium>`
   - `<label admin_sub_toggle_free>`
   - `<label admin_reset_own_limits>`
   - `<label admin_reset_limits>`
   - `<label admin_promo>`
   - `<label back>` — «Вернуться» (возврат в основной экран админки)

3. Действия:
   - `<label admin_sub_toggle_premium>`:
     - Переводит выбранному пользователю (или самому админу — если только self) тариф в премиум.
     - Сбрасывает лимиты.
   - `<label admin_sub_toggle_free>`:
     - Переводит в бесплатный тариф.
     - Сбрасывает лимиты.
   - `<label admin_reset_own_limits>`:
     - Сбрасывает лимиты самому админу.
   - `<label admin_reset_limits>`:
     - Переводит FSM в `STATE_LIMIT_RESET`.
     - Отправляет `<message admin_limit_reset_prompt>`:

       > Введите ID пользователя, которому сбросить лимит.

#### 7.2.1. Сброс лимита пользователю (`STATE_LIMIT_RESET`)

1. В `STATE_LIMIT_RESET` любое текстовое сообщение:
   - интерпретируется как user_id.
2. Бот проверяет:
   - существует ли такой пользователь в БД.
3. Если **нет**:
   - Отправляет `<message admin_user_not_found>`:

     > Пользователь не найден.

   - Остаётся в `STATE_LIMIT_RESET`.
4. Если **есть**:
   - Сбрасывает лимиты:
     - как будто пользователь не делал запросов за день.
   - Отправляет `<message admin_limits_reset>`:

     > Пользователю ID = {user_id} сброшен лимит.

   - Переводит обратно в `STATE_ADMIN`.

5. Также возможен выход по кнопке `<label back>` → `STATE_ADMIN`.


### 7.3. Генерация промокодов (`STATE_PROMO_GENERATE`)

1. Админ нажимает `<label admin_promo>` — «Промокод».
2. Бот:
   - Переводит в `STATE_PROMO_GENERATE`.
   - Отправляет `<message admin_promo_generate_prompt>`:

     > Сколько уникальных промокодов сгенерировать (1–10)?

3. В `STATE_PROMO_GENERATE` любое текстовое сообщение:
   - Проверяется как число 1–10.
4. Если не число или вне диапазона:
   - Бот повторяет `<message admin_promo_generate_prompt>`.
5. Если число корректное:
   - Бот генерирует N уникальных промокодов
   - Сохраняет их в БД (с параметрами: длительность премиума, дата истечения и т.п.)
   - Отправляет `<message admin_promo_generated>`:

     > Сгенерированы промокоды:  
     > CODE1  
     > CODE2  
     > ...

   - Переводит в `STATE_ADMIN`.

6. Выход по кнопке `<label back>` также возвращает в `STATE_ADMIN`.


### 7.4. Просмотр статистики (админ)

1. Админ нажимает `<label admin_statistics>`.
2. Бот показывает кнопки:
   - `<label stat_week>` — «За неделю»
   - `<label stat_month>` — «За месяц»
   - `<label stat_all_time>` — «За все время»
   - `<label back>` — «Вернуться»
3. После выбора периода:
   - Бот считает метрики за период и отправляет сводный отчет, например:

     ```text
     Статистика за период: 1–31 марта 2025

     Новых уникальных пользователей: N
     Активных пользователей: M
     Покупок премиум: K
     Отправлено фото: F
     Средний расход токенов GPT: T
     Доход от подписок: X
     Себестоимость GPT: Y
     Маржа: Z
     ```

4. Выход:
   - `<label back>` → основной экран админки (`STATE_ADMIN`)
   - `<label admin_exit>` → выход из админки, `STATE_STANDARD`.

